/* FS Simulator Styles */

#fs-status {
    padding: 1 1 1 1;
    color: $text-muted;
    height: auto;
}

#fs-breadcrumb {
    padding: 0 1 1 1;
    color: $accent;
    height: auto;
}

#fs-table {
    height: 1fr;
    width: 100%;
}

/* FS Simulator spacer - aligns with right panel bottom */
#fs-simulator-spacer {
    height: 4;
}
